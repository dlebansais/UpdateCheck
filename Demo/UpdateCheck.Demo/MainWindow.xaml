<Window x:Class="UpdateCheckDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update check of the UpdateCheck project"
        SizeToContent="WidthAndHeight"
        MinWidth="400">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Text="Has Update:" TextAlignment="Right" Margin="0,5,0,5"/>
        <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding IsUpdateAvailable}" Margin="5,5,0,5"/>
        <TextBlock Grid.Column="0" Grid.Row="1" Text="Version:" TextAlignment="Right" Margin="0,5,0,5"/>
        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding ReleasePageAddress }" Margin="5,5,0,5"/>
    </Grid>
</Window>
